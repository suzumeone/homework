#pragma once
#include <iostream>
#include <string>
#include <map>

#define NUMBER_LENGHT 8

bool checkNumber(std::string& number);
bool checkSurname(std::string& surname);
bool SeparateData(std::string& data, std::string& number, std::string& surname);
